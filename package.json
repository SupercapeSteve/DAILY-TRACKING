{
  "name": "daily-tracker",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:color": "esbuild src/lib/color.ts --bundle --format=esm --outfile=scripts/.tmp/color.bundle.mjs --log-level=warning && esbuild src/lib/theme.ts --bundle --format=esm --outfile=scripts/.tmp/theme.bundle.mjs --log-level=warning && node scripts/test-color.mjs",
    "check": "npm run typecheck && npm run test:color && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
